# beniget — Python 3.9

Runs beniget's def-use chain analysis over the real `catalog/pricing.py`
module (not a standalone fixture).

Run from repo root:

```bash
python quality/beniget/run_beniget.py
```
