# crosshair — Python 3.9

Symbolically checks `catalog/pricing.py::calculate_order_total` against
the pre/post contracts already in its docstring.

Run from repo root:

```bash
crosshair check catalog/pricing.py
```
