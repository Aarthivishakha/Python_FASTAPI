tool: crosshair
version: "0.0.109"
language: python
language_version: "3.9"
technique: "Static Analysis Metric"
classification: "Execution Path Integrity"
target_files:
  - ../../catalog/pricing.py
run:
  command: "crosshair check catalog/pricing.py"
requirements:
  - crosshair-tool>=0.0.109
expects:
  contracts_verified: true
notes: >
  calculate_order_total's docstring carries real pre/post contracts
  (pre: quantity > 0, pre: unit_price_cents >= 0, post: __return__ >= 0)
  that crosshair checks against the actual implementation.
