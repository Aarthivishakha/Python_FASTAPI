# radon + lizard — Python 3.9

Reports cyclomatic complexity for `catalog/pricing.py::classify_order_size`
and `calculate_order_total`.

Run from repo root:

```bash
radon cc catalog/pricing.py -s -a
lizard catalog/pricing.py
```
